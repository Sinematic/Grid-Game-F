import { MapType } from "../types/common"
import "../styles/MapReader.css"

export default function MapReader(props: { map : MapType }) {

    const { map } = props

    return (
        <div className="game-map">
            
        </div>
    )
}
